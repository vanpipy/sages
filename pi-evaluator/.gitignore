# Node / TypeScript
node_modules/
dist/
bun.lock
package-lock.json
yarn.lock

# Build output
.tmp/
tmp/

# Logs
*.log
logs/

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
.env.local

# Note: `lib/` is intentionally NOT ignored — src/lib/ contains the TypeScript
# artifact-reader + jsonl-reader modules. The Python `lib/` convention does
# not apply to this TypeScript project.

# Worktrees (isolated development)
.worktrees/
tool/
