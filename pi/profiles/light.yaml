id: light
description: In-process tool families only; ≤2-item workflows stay in main session.
subagents: [Explore]
isolation_default: none
dag_threshold: 1
gate_suite: []
soft_mode_reminder: ""
soft_mode_system_prompt_suffix: |
  ## Soft Mode (light profile active)

  This profile restricts dispatch to `Explore` only — read-only searches.
  For >2-item workflows, recommend a DAG, but subagent dispatch is limited
  to Explore. Direct handling is acceptable for any task in this profile.