# Active tooling — minimal

No additional tooling is configured for this profile. You have
access to the baseline tools: `bash`, `read`, `edit`, `write`,
`grep`, `find`, `ls`.

Solve tasks directly with these primitives.

See SYSTEM.md for hard rules (meta-file classification, foreground
vs background, TDD, commit conventions, namespace ownership).